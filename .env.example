# Copy to .env for local testing of scripts/fetch-intel.mjs — never commit the real .env.
# In production (GitHub Actions) this is set via repo Settings → Secrets and variables → Actions,
# not via a file.
ANTHROPIC_API_KEY=
